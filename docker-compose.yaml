version: '3.7'

services:
  demo-eureka-service:
    image: gmarino/demo-eureka:0.0.1-SNAPSHOT
    mem_limit: 700m
    ports:
      - "8080:8080"
    networks:
      - prova-network

networks:
  prova-network:
